<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color for ingredients items -->
    <solid android:color="@color/ingredients_item_bg" />

    <!-- Border stroke -->
    <stroke
        android:width="1dp"
        android:color="@color/ingredients_item_stroke" />

    <!-- Rounded corners -->
    <corners android:radius="8dp" />

</shape>
